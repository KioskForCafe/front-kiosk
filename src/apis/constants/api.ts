const HOST = 'http://localhost:3000/';

export const SIGN_UP_URL = `${HOST}auth/sign-up`;
export const SIGN_IN_URL = `${HOST}auth/sign-in`;

export const DUPLICATE_ID_URL = `${HOST}api/user/duplicate/userId`;
export const DUPLICATE_USERNAME_URL = `${HOST}api/user/duplicate/userName`;
export const DUPLICATE_TEL_NUMBER_URL = `${HOST}api/user/duplicate/tel-number`;